{
  "name": "df-clicker-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "start": "vite --port 3000",
    "build": "tsc && vite build",
    "copy": "mkdir -p ../pkg/df-clicker-ui && rm -rf ../pkg/df-clicker-ui/* && cp -r dist/* ../pkg/df-clicker-ui/",
    "build:copy": "npm run build && npm run copy",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dartfrog/puddle": "file:/Users/moth/repos/puddle",
    "react-router-dom": "^6.26.2",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "@types/react": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.0.0",
    "vite": "^5.4.4"
  }
}
